# Fresh upstream executions, 12 September 2026

These records first accompanied manuscript revision R3 and are preserved unchanged in R4. V24 ran its original `verify.py --jobs 4 --full-chain`; V28 ran its original `verify_v28_all.py`. They were executed in separate new task directories on X, at nice 10, with BLAS/OpenMP set to one thread. V24 used four workers and V28 one. The original archives and all manifest-listed static inputs were hash checked and unchanged.

V24 covers the 22 V20/V22/V24 exclusion trees and the earlier exact algebra/reconstruction checks actually invoked by its packaged dependency chain. V28 covers its complete 13-graft localization and all 11 required modules. The JSON files state the assumptions and scope of each run.

These are fresh executions of existing frozen acceptors, not newly implemented independent acceptors or a replay of every source of the global theorem. The analytic source-to-root implications are audited separately in the revised manuscript and accompanying priority-response packet. The user-reported separate cold run has now been identified as the D162 first-phase Lean project cold rebuild. Its completed records are in ../lean_cold_build/. It did not perform an additional global mathematical replay of the large certificates.

Historical strings such as `macro_ledger=11/15` or `FINAL alpha WALL THEOREM: NOT PROVED` are preserved exactly as printed by the older scoped verifiers. They describe the original package scope, not a rollback of the later research ledger. No old output is edited to make it appear to prove the full theorem.

The V28 first launch occurred before its runner upload completed and failed before the original verifier started. The confirmed upload was followed by one successful complete execution. The accompanying audit packet preserves both launcher logs; this directory contains the successful mathematical execution and its receipt.
